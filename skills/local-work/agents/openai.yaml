interface:
  display_name: "Local Work"
  short_description: "Plan and implement a spec without GitHub issue overhead"
  default_prompt: "Use $local-work to split this authoritative spec into local work items and implement them sequentially."
policy:
  allow_implicit_invocation: false
