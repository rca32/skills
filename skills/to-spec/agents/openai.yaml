interface:
  display_name: "Conversation to Spec"
  short_description: "Create a precise spec and separate human explainer"
  default_prompt: "Use $to-spec to turn this settled discussion into a precise spec and separate non-normative human explainer."
policy:
  allow_implicit_invocation: false
