interface:
  display_name: "Two-Axis Code Review"
  short_description: "Run fresh two-axis reviews against standards and spec"
  default_prompt: "Use $code-review with fresh candidate-scoped reviewers to assess the worktree against Standards and Spec."
